import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-6-http',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
